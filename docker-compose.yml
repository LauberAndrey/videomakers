version: '3.8'

services:
  react-app:
    build: .
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /etc/ssl/cert.pem:/etc/ssl/cert.pem
      - /etc/ssl/privkey.pem:/etc/ssl/privkey.pem
    restart: always
